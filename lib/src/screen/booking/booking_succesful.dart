import 'package:flutter/material.dart';

class BookingSuccesful extends StatelessWidget {
  const BookingSuccesful({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
